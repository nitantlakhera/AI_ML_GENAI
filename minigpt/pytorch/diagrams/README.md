# MiniGPT — Diagram Images (PNG)

These PNGs are rendered from the Mermaid diagrams embedded in the project's docs.
They were generated by `../render_diagrams.py` using the mermaid.ink service.

To regenerate (after editing any diagram in the docs):

```bash
python render_diagrams.py
```

---

## Index

### Neural network architecture — from `NEURAL_NETWORK.md`
| File | Shows |
|------|-------|
| `NEURAL_NETWORK__01__flowchart-td.png` | Full MiniGPT model (embeddings → 2 blocks → head) |
| `NEURAL_NETWORK__02__flowchart-td.png` | Inside ONE transformer block (attention + feed-forward) |

### Call / data flow — from `FLOW.md`
| File | Shows |
|------|-------|
| `FLOW__01__flowchart-lr.png` | Big picture: TRAIN mode vs USE mode |
| `FLOW__02__flowchart-td.png` | File dependency graph (who imports whom) |
| `FLOW__03__flowchart-td.png` | Training flow (`python train.py`) |
| `FLOW__04__sequencediagram.png` | What one training step does (forward/backward/step) |
| `FLOW__05__flowchart-td.png` | Forward pass (`model(x)`) |
| `FLOW__06__flowchart-td.png` | Generation / use flow |
| `FLOW__07__sequencediagram.png` | Chat call flow (user ↔ chat ↔ model) |
| `FLOW__08__flowchart-lr.png` | End-to-end quick reference |

> MiniGPT-vs-ChatGPT diagrams (`COMPARISON__*.png`) now live in `../../common/diagrams/`
> because that comparison is framework-agnostic.

### Docker — from `DOCKER.md`
| File | Shows |
|------|-------|
| `DOCKER__01__flowchart-lr.png` | Docker workflow (build → train → chat) |
